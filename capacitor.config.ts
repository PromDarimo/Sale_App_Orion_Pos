import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'smart pos e-menu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
